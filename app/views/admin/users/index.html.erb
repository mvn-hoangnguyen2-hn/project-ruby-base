<h1 class="mt-0">User List</h1>

<%= link_to 'Create New User', new_admin_user_path %>


<table border="1" cellpadding="10">
  <thead>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Age</th>
    <th>Phone</th>
    <th>Cars</th>
    <th>Actions</th>
  </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= user.email %></td>
      <td><%= user.age %></td>
      <td><%= user.phone %></td>
      <td>
        <%= link_to "View Cars", admin_cars_for_user_path(user.id) %>
      </td>
      <td>
        <%= link_to 'Edit', edit_admin_user_path(user) %> |
        <%= link_to 'Delete', admin_user_path(user), method: :delete, data: { confirm: 'Are you sure?' } %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>
